import { ToFirst10Pipe } from './to-first10.pipe';

describe('ToFirst10Pipe', () => {
  it('create an instance', () => {
    const pipe = new ToFirst10Pipe();
    expect(pipe).toBeTruthy();
  });
});
