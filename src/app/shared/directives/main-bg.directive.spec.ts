import { MainBgDirective } from './main-bg.directive';

describe('MainBgDirective', () => {
  it('should create an instance', () => {
    const directive = new MainBgDirective();
    expect(directive).toBeTruthy();
  });
});
